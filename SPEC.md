# Active / Inactive User Dashboard (RADIUS)

## 📌 ภาพรวมโปรเจกต์ (Project Overview)
โปรเจกต์นี้เป็นระบบ **Dashboard สำหรับแสดงสถานะการใช้งานของผู้ใช้ (Active / Inactive)**  
โดยอ้างอิงจากข้อมูล **RADIUS Accounting** ในตาราง `radacct`  
ใช้เพื่อการ **ตรวจสอบการใช้งานบัญชีผู้ใช้, ความมั่นคงปลอดภัย, และการปฏิบัติตามข้อกำหนด (Compliance)**

ระบบนี้เหมาะสำหรับใช้งานในบริบท:
- VPN
- Wi-Fi Enterprise
- Network Access Control (NAC)
- ระบบที่ใช้ RADIUS Authentication

---

## 🎯 วัตถุประสงค์ (Objectives)
1. แสดงจำนวนผู้ใช้งานที่ **ยังมีการใช้งาน (Active)**
2. ระบุผู้ใช้ที่ **ไม่ได้ใช้งานเกินระยะเวลาที่กำหนด (Inactive)**
3. สนับสนุนการทำ:
   - Account review
   - Cleanup / Disable account
   - Security audit
   - PDPA / ISO 27001 / NIST CSF compliance

---

## 🧠 นิยามที่ใช้ในระบบ (Definitions)

### Active User
- ผู้ใช้ที่มีการใช้งานระบบภายใน **90 วันล่าสุด**
- ตรวจสอบจากค่า `MAX(radacct.acctstarttime)`

### Inactive User
- ผู้ใช้ที่ **ไม่เคยใช้งานเลย**
- หรือ **ไม่ได้ใช้งานเกิน 90 วัน**

> หมายเหตุ: ระยะเวลา (เช่น 30 / 60 / 90 วัน) สามารถปรับได้ตามนโยบายองค์กร

---

## 🗄️ แหล่งข้อมูล (Data Sources)

### ตารางหลัก
- `rm_users`  
  ใช้เป็น **User master / Account list**

- `radacct`  
  ใช้เป็น **RADIUS Accounting Log**  
  เก็บข้อมูล session การใช้งานจริงของผู้ใช้ เช่น:
  - เวลาเริ่มใช้งาน
  - เวลาสิ้นสุด
  - ระยะเวลาการใช้งาน
  - IP ที่ได้รับ

---

## 🔍 Logic หลักของระบบ (Core Logic)

1. รวมข้อมูลผู้ใช้จาก `rm_users`
2. Join กับ `radacct` ด้วย `username`
3. คำนวณ **last_used** จาก `MAX(acctstarttime)`
4. แปลงค่าเป็นสถานะ:
   - active
   - inactive
5. ส่งผลลัพธ์ไปแสดงใน dashboard

---

## 📊 องค์ประกอบของ Dashboard (Dashboard Components)

### KPI Cards
- จำนวน Active Users
- จำนวน Inactive Users
- สัดส่วนผู้ใช้งาน (%)

### Charts
- จำนวนผู้ใช้งานรายวัน (Login Trend)
- แนวโน้มการใช้งานย้อนหลัง (30 / 90 วัน)

### Tables
- รายชื่อผู้ใช้ที่ Inactive
- วันที่ใช้งานครั้งล่าสุด
- จำนวนวันที่ไม่ได้ใช้งาน

---

## 🔐 มุมมองด้านความมั่นคงปลอดภัย (Security Perspective)

ระบบนี้ช่วยสนับสนุน:
- การลดความเสี่ยงจาก **Ghost Account**
- การบังคับใช้หลัก **Least Privilege**
- การตรวจสอบสิทธิ์การเข้าถึงเป็นระยะ (Access Review)

---

## 📑 Compliance Mapping (ตัวอย่าง)
- NIST CSF  
  - Identify: Account inventory  
  - Protect: Account lifecycle management  

- ISO/IEC 27001  
  - A.9 User access management

- PDPA  
  - ลดบัญชีผู้ใช้ที่ไม่จำเป็นต่อการประมวลผลข้อมูล

---

## 🚀 การต่อยอดในอนาคต (Future Enhancements)
- แยกประเภทผู้ใช้ (VPN / WiFi / Admin)
- กำหนดระดับความเสี่ยง (30 / 60 / 90 วัน)
- Auto-disable หรือแจ้งเตือนผู้ดูแลระบบ
- Export รายงานเป็น Excel / PDF
- เชื่อมกับระบบ SIEM หรือ SOC dashboard

---

## 🧩 กลุ่มเป้าหมายของระบบ (Target Users)
- Network Administrator
- Security / SOC Team
- IT Auditor
- ผู้บริหารที่ต้องการภาพรวมการใช้งานบัญชีผู้ใช้

---

## 📝 หมายเหตุสำหรับ AI / Automation
ไฟล์นี้ออกแบบมาให้ AI หรือระบบอัตโนมัติสามารถ:
- เข้าใจบริบทของโปรเจกต์ได้โดยไม่ต้องอ่านโค้ด
- ใช้เป็นฐานสำหรับการสร้าง SQL, API, หรือ Dashboard เพิ่มเติม
- ใช้เป็น prompt context สำหรับ AI ตัวอื่นได้ทันที


